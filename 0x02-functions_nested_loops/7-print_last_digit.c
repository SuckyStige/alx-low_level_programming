#include "main.h"

/**
 * main - check the code
 *
 * Return: Always 0.
 */

int print_last_digit(int num)
{
	int pwd;
        num(98);
       	num(0);
       	pwd = num(-1024);
	_putchar(num + pwd);
        _putchar('\n');
	return (0);
}
