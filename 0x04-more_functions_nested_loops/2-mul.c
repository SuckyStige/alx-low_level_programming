#include "main.h"

/**
 * mul - Multiplies two integers
 * @u: The first number to multiply
 * @b: The second number to multply with first number
 *
 * Return: The result of multiplication between u param and b param
 */
int mul(int u, int b)
{
	return (u * b);
}
